package com.mohdshayan.kickset.ui.theme

import androidx.compose.ui.graphics.Color

/*
 * Layout blue and mill steel: layout dye on hot-rolled plate. One accent, Layout Blue, on every screen.
 * Names follow blueprint section 7. Background tokens match res/values colors.xml so the first frame agrees.
 */

// Light mode
val LightBackground = Color(0xFFEEF1F4) // Plate
val LightSurface = Color(0xFFF7F9FB)    // Sheet
val LightInk = Color(0xFF16202B)        // Ink
val LightScale = Color(0xFF4F5B67)      // Scale
val LightAccent = Color(0xFF2152B0)     // Layout Blue
val LightOnAccent = Color(0xFFF7F9FB)   // On Blue
val LightRule = Color(0xFFC9D1D9)       // Rule
val LightError = Color(0xFFA8322A)      // Weld Red
val LightSelection = Color(0xFFDCE4F4)  // Layout Blue wash under a selected chip or row
val LightErrorWash = Color(0xFFF6E4E2)  // Weld Red wash behind an error container

// Dark mode
val DarkBackground = Color(0xFF10161D)
val DarkSurface = Color(0xFF18212A)
val DarkInk = Color(0xFFE3E8ED)
val DarkScale = Color(0xFF9AA6B2)
val DarkAccent = Color(0xFF86A8EA)
val DarkOnAccent = Color(0xFF0E1A2E)
val DarkRule = Color(0xFF2A3540)
val DarkError = Color(0xFFF08A80)
val DarkSelection = Color(0xFF223250)
val DarkErrorWash = Color(0xFF3A2422)
